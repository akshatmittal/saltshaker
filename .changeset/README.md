# Changesets

Run `pnpm changeset` to add a release note for `@akshatmittal/saltshaker`.

When changesets land on `main`, GitHub Actions will either:

- open or update a release PR with version bumps and changelog state, or
- publish to npm if there are pending changesets and the release PR has already been merged.
